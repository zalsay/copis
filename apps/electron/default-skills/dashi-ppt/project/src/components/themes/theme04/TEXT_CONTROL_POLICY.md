# Theme04 Text Control Policy

Theme04 keeps `text`, `string`, `input`, `url`, `email`, `textarea`, and `multiline` controls so imported slides remain editable from the sidebar.

Other imported themes continue to filter those control types from public prop controls. The policy is pinned by `scripts/test/test-jad25x-theme04-05-contracts.mjs`.
