# dependencies (bun install)
node_modules

# output
out
dist
*.tgz
native/**/target/

# 编译产物：copis CLI 二进制（build:cli 用 bun build --compile 生成，打包时现编，不进 git）
apps/electron/resources/bin/
# Swift 编译的 macOS 原生灵动岛 helper（构建时生成，按宿主架构分发）
apps/electron/resources/agent-island/
# 默认 Pi 扩展副本（copy:pi-extensions 从 node_modules 生成，打包时现拷，不进 git）
apps/electron/resources/pi-extensions/
# 工作区项目 Node runtime 功能模块归档（build:node-runtime-module 生成，打包时现拷，不进 git）
apps/electron/resources/node-runtime/
apps/electron/resources/alipay-bot/
apps/electron/resources/playwright-core/
# 根目录误提交的 esbuild 打包产物（真实入口是 apps/electron/dist/main.cjs）
/main.cjs
# bun build --compile 的临时中间产物
*.bun-build

# code coverage
coverage
*.lcov

# logs
logs
_.log
report.[0-9]_.[0-9]_.[0-9]_.[0-9]_.json

# dotenv environment variable files
.env
.env.development.local
.env.test.local
.env.production.local
.env.local

# caches
.eslintcache
.cache
*.tsbuildinfo

# IntelliJ based IDEs
.idea

# Finder (MacOS) folder config
.DS_Store

# ignore craft-agents-oss folder
craft-agents-oss

# ignore what_are_humans_thinking folder
what_are_humans_thinking

# vendor binaries
vendor

# git worktrees (managed by copis-worktree skill)
.worktrees
# pnpm (本项目用 bun，禁止误提交 pnpm lockfile)
pnpm-lock.yaml
pnpm-workspace.yaml

# Copis 会话/工作区文档（不应入仓，见 CLAUDE.md 红线）
.context/
.pi/
.env
.superpowers/
.agents/
skills-lock.json
